# We creates a dict() called countryMap. 
countryMap = dict()

# The KEYS are the name of a country, and the VALUE for each key is the name of that
# country's capital city.
countryMap['UnitedKingdom'] = 'London'
countryMap['Sweden'] = 'Stockholm'
countryMap['Canada'] = 'Ottawa'

print(countryMap['Sweden'])