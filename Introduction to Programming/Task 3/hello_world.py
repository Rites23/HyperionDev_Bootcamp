# User requested to Enter their name. That is then stored in a "name" variable. 
# We use int as the age would be a integer. That is then stored in a "age" variable. 
name = input ("Enter your name?") 
print (name) 
age = int(input("Enter your age?"))
print (age)
print ("Hello World!")
 